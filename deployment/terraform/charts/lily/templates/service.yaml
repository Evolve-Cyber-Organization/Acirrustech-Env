apiVersion: v1
kind: Service
metadata:
  name: {{ include "lily.fullname" . }}
  labels:
    {{- include "lily.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "lily.selectorLabels" . | nindent 4 }}
