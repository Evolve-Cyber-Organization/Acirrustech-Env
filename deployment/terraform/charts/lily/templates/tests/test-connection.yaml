apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "lily.fullname" . }}-test-connection"
  labels:
    {{- include "lily.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "lily.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
