apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "andrii-chart.fullname" . }}-test-connection"
  labels:
    {{- include "andrii-chart.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "andrii-chart.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
