apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "artemis-gulmira-chart.fullname" . }}-test-connection"
  labels:
    {{- include "artemis-gulmira-chart.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "artemis-gulmira-chart.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
