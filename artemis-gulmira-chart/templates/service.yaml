apiVersion: v1
kind: Service
metadata:
  name: {{ include "artemis-gulmira-chart.fullname" . }}
  labels:
    {{- include "artemis-gulmira-chart.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "artemis-gulmira-chart.selectorLabels" . | nindent 4 }}
