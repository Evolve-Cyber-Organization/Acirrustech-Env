apiVersion: v1
kind: Service
metadata:
  name: {{ include "aizada-artemis.fullname" . }}
  labels:
    {{- include "aizada-artemis.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "aizada-artemis.selectorLabels" . | nindent 4 }}
