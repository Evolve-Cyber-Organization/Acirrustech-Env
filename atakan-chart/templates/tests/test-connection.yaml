apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "atakan-chart.fullname" . }}-test-connection"
  labels:
    {{- include "atakan-chart.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "atakan-chart.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
