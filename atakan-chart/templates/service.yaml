apiVersion: v1
kind: Service
metadata:
  name: {{ include "atakan-chart.fullname" . }}
  labels:
    {{- include "atakan-chart.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "atakan-chart.selectorLabels" . | nindent 4 }}
