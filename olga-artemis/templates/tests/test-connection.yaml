apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "olga-artemis.fullname" . }}-test-connection"
  labels:
    {{- include "olga-artemis.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "olga-artemis.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
