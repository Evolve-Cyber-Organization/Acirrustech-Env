apiVersion: apps/v1
kind: Deployment
metadata:
  name: artemis-parviz1
  labels:
    app: artemis-parviz1
spec:
  replicas: 1
  selector:
    matchLabels:
      app: artemis-parviz1
  template:
    metadata:
      labels:
        app: artemis-parviz1
    spec:
      containers:
      - name: artemis-parviz1
        image: "{{ .Values.image.repository }}:{{ .Values.image.tag }}"
        ports:
        - containerPort: 5000