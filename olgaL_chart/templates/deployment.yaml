
apiVersion: apps/v1
kind: Deployment
metadata:
  name: artemis-olgaL
  labels:
    app: artemis-olgaL
spec:
  replicas: 1
  selector:
    matchLabels:
      app: artemis-olgaL
  template:
    metadata:
      labels:
        app: artemis-olgaL
    spec:
      containers:
      - name: artemis
        image: "{{ .Values.image.repository }}:{{ .Values.image.tag }}"
        ports:
        - containerPort: 5000