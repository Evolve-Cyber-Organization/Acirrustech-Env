apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "artemislily.fullname" . }}-test-connection"
  labels:
    {{- include "artemislily.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "artemislily.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
