  
apiVersion: v1
kind: Service
metadata:
  name: {{ include "laziz.fullname" . }}
  labels:
    {{- include "laziz.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "laziz.selectorLabels" . | nindent 4 }}