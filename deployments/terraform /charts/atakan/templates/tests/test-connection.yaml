apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "atakan.fullname" . }}-test-connection"
  labels:
    {{- include "atakan.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "atakan.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
