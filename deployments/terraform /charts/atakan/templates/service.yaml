apiVersion: v1
kind: Service
metadata:
  name: {{ include "atakan.fullname" . }}
  labels:
    {{- include "atakan.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "atakan.selectorLabels" . | nindent 4 }}
