apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "sezgin.fullname" . }}-test-connection"
  labels:
    {{- include "sezgin.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "sezgin.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
