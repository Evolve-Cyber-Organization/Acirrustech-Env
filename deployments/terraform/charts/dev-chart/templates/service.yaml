apiVersion: v1
kind: Service
metadata:
  name: {{ include "sezgin.fullname" . }}
  labels:
    {{- include "sezgin.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "sezgin.selectorLabels" . | nindent 4 }}
