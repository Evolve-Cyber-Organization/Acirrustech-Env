apiVersion: v1
kind: Service
metadata:
  name: {{ include "gulmira-example.fullname" . }}
  labels:
    {{- include "gulmira-example.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "gulmira-example.selectorLabels" . | nindent 4 }}
