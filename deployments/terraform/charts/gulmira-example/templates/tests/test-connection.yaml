apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "gulmira-example.fullname" . }}-test-connection"
  labels:
    {{- include "gulmira-example.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "gulmira-example.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
