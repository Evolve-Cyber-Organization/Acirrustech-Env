apiVersion: v1
kind: Service
metadata:
  name: {{ include "merves-chart.fullname" . }}
  labels:
    {{- include "merves-chart.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "merves-chart.selectorLabels" . | nindent 4 }}
