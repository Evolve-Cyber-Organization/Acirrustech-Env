apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "merves-chart.fullname" . }}-test-connection"
  labels:
    {{- include "merves-chart.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "merves-chart.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
