apiVersion: v1
kind: Service
metadata:
  name: {{ include "olgaoj.fullname" . }}
  labels:
    {{- include "olgaoj.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "olgaoj.selectorLabels" . | nindent 4 }}
