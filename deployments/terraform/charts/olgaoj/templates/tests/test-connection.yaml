apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "olgaoj.fullname" . }}-test-connection"
  labels:
    {{- include "olgaoj.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "olgaoj.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
