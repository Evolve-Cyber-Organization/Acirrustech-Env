apiVersion: v1
kind: Service
metadata:
  name: {{ include "artmis-sezgin.fullname" . }}
  labels:
    {{- include "artmis-sezgin.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "artmis-sezgin.selectorLabels" . | nindent 4 }}