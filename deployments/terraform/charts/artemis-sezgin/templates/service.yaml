apiVersion: v1
kind: Service
metadata:
  name: {{ include "artemis-sezgin.fullname" . }}
  labels:
    {{- include "artemis-sezgin.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "artemis-sezgin.selectorLabels" . | nindent 4 }}