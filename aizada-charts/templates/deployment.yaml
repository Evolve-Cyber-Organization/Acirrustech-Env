apiVersion: apps/v1
kind: Deployment
metadata:
  name: artemis-aizada
  labels:
    app: artemis-aizada
spec:
  replicas: 1
  selector:
    matchLabels:
      app: artemis-aizada
  template:
    metadata:
      labels:
        app: artemis-aizada
    spec:
      containers:
      - name: artemis-aizada
        image: "{{ .Values.image.repository }}:{{ .Values.image.tag }}"
        ports:
        - containerPort: 5000