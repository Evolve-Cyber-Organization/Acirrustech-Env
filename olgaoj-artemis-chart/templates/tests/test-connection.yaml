apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "olgaoj-artemis-chart.fullname" . }}-test-connection"
  labels:
    {{- include "olgaoj-artemis-chart.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "olgaoj-artemis-chart.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
