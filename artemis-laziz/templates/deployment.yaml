piVersion: apps/v1
kind: Deployment
metadata:
  name: artemis-laziz
  labels:
    app: artemis-laziz
spec:
  replicas: 1
  selector:
    matchLabels:
      app: artemis-laziz
  template:
    metadata:
      labels:
        app: artemis-laziz
    spec:
      containers:
      - name: artemis-laziz
        image: "{{ .Values.image.repository }}:{{ .Values.image.tag }}"
        ports:
        - containerPort: 5000