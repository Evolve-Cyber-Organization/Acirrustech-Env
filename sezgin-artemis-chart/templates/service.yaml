apiVersion: v1
kind: Service
metadata:
  name: {{ include "sezgin-artemis-chart.fullname" . }}
  namespace: {{ .Values.Namespace }}
  labels:
    {{- include "sezgin-artemis-chart.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "sezgin-artemis-chart.selectorLabels" . | nindent 4 }}
