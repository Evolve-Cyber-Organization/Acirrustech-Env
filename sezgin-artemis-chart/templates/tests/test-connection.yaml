apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "sezgin-artemis-chart.fullname" . }}-test-connection"
  labels:
    {{- include "sezgin-artemis-chart.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "sezgin-artemis-chart.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
