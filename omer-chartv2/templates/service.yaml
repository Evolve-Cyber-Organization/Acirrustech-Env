apiVersion: v1
kind: Service
metadata:
  name: {{ include "omer-chartv2.fullname" . }}
  labels:
    {{- include "omer-chartv2.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "omer-chartv2.selectorLabels" . | nindent 4 }}
