apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "omer-chartv2.fullname" . }}-test-connection"
  labels:
    {{- include "omer-chartv2.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "omer-chartv2.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
