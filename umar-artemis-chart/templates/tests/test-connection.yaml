apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "umar-artemis.fullname" . }}-test-connection"
  labels:
    {{- include "umar-artemis.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "umar-artemis.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never

