apiVersion: v1
kind: Service
metadata:
  name: {{ include "umar-artemis.fullname" . }}
  labels:
    {{- include "umar-artemis.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "umar-artemis.selectorLabels" . | nindent 4 }}