apiVersion: v1
kind: Service
metadata:
  name: {{ include "lilyart.fullname" . }}
  labels:
    {{- include "lilyart.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "lilyart.selectorLabels" . | nindent 4 }}
