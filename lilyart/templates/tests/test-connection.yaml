apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "lilyart.fullname" . }}-test-connection"
  labels:
    {{- include "lilyart.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "lilyart.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
