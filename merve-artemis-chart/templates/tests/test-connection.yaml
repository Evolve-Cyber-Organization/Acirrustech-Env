apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "merve-artemis-chart.fullname" . }}-test-connection"
  labels:
    {{- include "merve-artemis-chart.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "merve-artemis-chart.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
