apiVersion: v1
kind: Service
metadata:
  name: {{ include "merve-artemis-chart.fullname" . }}
  labels:
    {{- include "merve-artemis-chart.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "merve-artemis-chart.selectorLabels" . | nindent 4 }}
