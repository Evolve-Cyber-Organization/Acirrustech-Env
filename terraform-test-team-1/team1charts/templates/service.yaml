apiVersion: v1
kind: Service
metadata:
  name: {{ include "team1charts.fullname" . }}
  labels:
    {{- include "team1charts.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "team1charts.selectorLabels" . | nindent 4 }}
